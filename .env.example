# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=asap
DB_USERNAME=asap
DB_PASSWORD=asap
DB_MAX_POOL_SIZE=20
DB_MIN_IDLE=5
DB_IDLE_TIMEOUT=300000
DB_CONNECTION_TIMEOUT=20000

# JWT - MUST be changed for production (256+ bits)
JWT_SECRET=CHANGE_ME_generate_a_random_256bit_secret_key_here
JWT_ACCESS_TOKEN_VALIDITY=3600000
JWT_REFRESH_TOKEN_VALIDITY=604800000

# API Keys
API_KEYS_SECRET=CHANGE_ME_generate_a_random_secret_key_here

# OAuth2
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Email (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=

# App Config
APP_BASE_API_URL=http://localhost:8080
APP_BASE_UI_URL=http://localhost:3000
APP_MAIL_FROM=no-reply@example.com
APP_MAIL_NAME=ASAP
APP_LINK_EXPIRATION_MINUTES=60
APP_ATTACHMENT_EXPIRATION_DAYS=7

# Rate Limiting
APP_RATE_LIMIT_ENABLED=true
APP_RATE_LIMIT_RPS=15

# File Storage (S3/MinIO)
AWS_ACCESS_KEY=asap
AWS_SECRET_KEY=asap
AWS_REGION=us-east-1
AWS_ENDPOINT=http://localhost:9000
AWS_S3_BUCKET=asap
AWS_S3_PRESIGNED_URL_ENDPOINT=http://localhost:9000
AWS_S3_PRESIGNED_URL_EXPIRATION_MINUTES=60

# Liquibase
LIQUIBASE_ENABLED=true

# Spring
SPRING_PROFILES_ACTIVE=local
JPA_SHOW_SQL=false

# NVD (for OWASP dependency check)
NVD_API_KEY=
